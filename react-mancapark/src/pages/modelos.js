import React from "react";

const Modelos = ({}) => {

    // ----

    return (
        <>
        <header>
            <span>{"Modelos"}</span>
        </header>
        </>
    );
};

export default Modelos;